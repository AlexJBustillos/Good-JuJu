import React, { Component } from 'react'
import Client from 'shopify-buy'

const client = React.build

class ShopProvider extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default ShopProvider